{
    "db": {
        "type": "mysql",

        "mysql": {
            "host": "localhost",
            "user": "aws_deploy",
            "password": "aws_deploy",
            "database": "aws_deploy",
            "charset": "utf8",
            "limit": 10,
            "debug": false
        }
    },

    "session": {
        "secret": "SECRET",
        "store": "memory",

        "redis": {
        }
    },

    "endpoints": {
        "http": {
            "listen": "0.0.0.0:4040"
        }
    },

    "users": {
        "register": false
    },

    "aws": {
        "region": null,
        "key": null,
        "secret": null
    }
}
